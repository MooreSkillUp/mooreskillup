# Nginx

Optional reverse-proxy examples live here for self-hosted deployments.

## Intended use

- route `/` to the frontend
- route `/api/` to the Django backend
- terminate TLS if Front Door is not used

